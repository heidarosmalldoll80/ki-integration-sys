const config = require('../config/config');

exports.getExampleData = (req, res) => {
    // Placeholder für die Anbindung an einen KI-Dienst
    const data = { example: 'Beispiel-Daten von einem KI-Dienst' };
    res.json(data);
};
